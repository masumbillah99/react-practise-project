import { createContext } from "react";

/** file name index.js deoyar mane holo jate sobgula context file 
    alada alada file a create kore and export korar theke -- borong
    sob gula export context akta file a create and export kora jai
*/

const MovieContext = createContext(null);

export { MovieContext };
